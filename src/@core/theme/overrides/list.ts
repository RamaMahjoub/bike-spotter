const List = {
    MuiList: {
        styleOverrides: {
            root: () => ({
                display: 'flex',
                flexDirection: 'column',
                placeItems: 'center',
                width: '100%'
            })
        }
    }
}

export default List
